import { IFlagshipConfig } from '../config/index';
import { CampaignDTO, primitive } from '../types';
import { IHttpClient } from '../utils/HttpClient';
import { MurmurHash } from '../utils/MurmurHash';
import { VisitorAbstract } from '../visitor/VisitorAbstract';
import { DecisionManager } from './DecisionManager';
export declare class BucketingManager extends DecisionManager {
    private _lastModified;
    private _isPooling;
    private _murmurHash;
    private _isFirstPooling;
    private _intervalID;
    constructor(httpClient: IHttpClient, config: IFlagshipConfig, murmurHash: MurmurHash);
    private finishLoop;
    startPolling(): Promise<void>;
    private polling;
    stopPolling(): void;
    private sendContext;
    getThirdPartySegment(visitorId: string): Promise<Record<string, primitive>>;
    getCampaignsAsync(visitor: VisitorAbstract): Promise<CampaignDTO[] | null>;
    private getVisitorCampaigns;
    private getVariation;
    private isMatchTargeting;
    private isANDListOperator;
    private checkAndTargeting;
    private testListOperatorLoop;
    private testListOperator;
    private testOperator;
}
