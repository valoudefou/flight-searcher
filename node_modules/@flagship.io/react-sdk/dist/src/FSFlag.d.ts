import { IFSFlag, IFSFlagMetadata, FSFlagStatus } from '@flagship.io/js-sdk';
import { FsContextState } from './type';
export declare class FSFlag implements IFSFlag {
    private key;
    private flag?;
    constructor(key: string, state: FsContextState);
    getValue<T>(defaultValue: T): (T extends null ? unknown : T);
    exists(): boolean;
    visitorExposed(): Promise<void>;
    get metadata(): IFSFlagMetadata;
    get status(): FSFlagStatus;
}
